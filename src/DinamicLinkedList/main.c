#include <stdio.h>

int main()
{
    int elements[] = { 12, 6, 4, 15, 10, 8 };
    int length = sizeof(elements) / sizeof(elements[0]);
    //LinkedList* list = linked_newList();

    // linked_addElements(list, elements, length);

    // printf("This list contains %i elements \n\n", linked_length(list));

    // linked_showElements(list);

    // linked_removeElement(list, 12);

    // linked_showElements(list);

    // int elementIndex = linked_sequencialSearch(list, 8);
    // (elementIndex < 0) ? printf("Element not found \n") : printf("Element found at index %i \n", elementIndex);

    // printf("This list contains %i elements \n\n", list.length);

    return 0;
}